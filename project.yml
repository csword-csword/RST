name: RST
options:
  bundleIdPrefix: com.rst
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
targets:
  RST:
    type: application
    platform: iOS
    sources:
      - RST
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.rst.app
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_CFBundleDisplayName: RST
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_NSCameraUsageDescription: "RST uses the camera to identify gym machines and read the weight stack."
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: "RST records where your workouts take place."
        INFOPLIST_KEY_NSBluetoothAlwaysUsageDescription: "RST connects to the smart pin device on weight machines to track your sets and reps."
  RSTTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - RSTTests
    dependencies:
      - target: RST
    settings:
      base:
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/RST.app/RST"
        BUNDLE_LOADER: "$(TEST_HOST)"
schemes:
  RST:
    build:
      targets:
        RST: all
    run:
      config: Debug
    test:
      targets:
        - RSTTests
